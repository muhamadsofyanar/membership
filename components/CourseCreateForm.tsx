"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export function CourseCreateForm(){const [loading,setLoading]=useState(false);const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/admin/courses",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});setLoading(false);if(!res.ok){setError((await res.json()).error||"Gagal membuat kursus.");return}(e.target as HTMLFormElement).reset();router.refresh()}return <form onSubmit={submit}>{error&&<div className="alert alert-error">{error}</div>}<div className="field"><label>Judul kursus</label><input name="title" required/></div><div className="field"><label>Deskripsi</label><textarea name="description" required rows={3}/></div><div className="field"><label>Judul modul pertama</label><input name="moduleTitle" required placeholder="Contoh: Pengenalan"/></div><div className="field"><label>Judul materi pertama</label><input name="lessonTitle" required/></div><div className="field"><label>Isi materi</label><textarea name="content" required rows={7}/></div><div className="field"><label>URL video embed (opsional)</label><input name="videoUrl" placeholder="https://www.youtube.com/embed/..."/></div><button className="btn btn-primary" disabled={loading}>{loading?"Membuat...":"Buat dan Terbitkan"}</button></form>}
