"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function AdminOrderActions({id}:{id:string}){const [loading,setLoading]=useState(false);const router=useRouter();async function act(status:"PAID"|"REJECTED"){if(!confirm(status==="PAID"?"Setujui pembayaran dan aktifkan membership?":"Tolak pembayaran ini?"))return;setLoading(true);const res=await fetch(`/api/admin/orders/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status})});setLoading(false);if(!res.ok)alert("Tindakan gagal.");router.refresh()}return <div className="flex"><button disabled={loading} onClick={()=>act("PAID")} className="btn btn-primary btn-sm">Setujui</button><button disabled={loading} onClick={()=>act("REJECTED")} className="btn btn-ghost btn-sm">Tolak</button></div>}
