"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export function CheckoutForm({planId}:{planId:string}){const [error,setError]=useState("");const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");try {const form=new FormData(e.currentTarget);form.set("planId",planId);const res=await fetch("/api/orders",{method:"POST",body:form});const body=await res.json();setLoading(false);if(!res.ok){setError(body.error||"Gagal mengirim pembayaran.");return}router.push("/dashboard/orders?sent=1");router.refresh()}catch{setError("Koneksi gagal. Silakan coba lagi.")}finally{setLoading(false)}}return <form onSubmit={submit}>{error&&<div className="alert alert-error">{error}</div>}<div className="field"><label>Rekening pengirim / nama pemilik</label><input name="transferAccount" required placeholder="Contoh: BCA 1234 a.n. Budi"/></div><div className="field"><label>Bukti transfer</label><input name="proof" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" required/><small className="muted">JPG, PNG, WebP, atau PDF. Maksimal 2 MB.</small></div><div className="field"><label>Catatan (opsional)</label><textarea name="notes" rows={3} placeholder="Informasi tambahan untuk admin"/></div><button className="btn btn-primary btn-block" disabled={loading}>{loading?"Mengirim...":"Kirim Bukti Pembayaran"}</button></form>}
