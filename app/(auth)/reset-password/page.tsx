import {ActionForm} from "@/components/ActionForm";import Link from "next/link";
export default async function Reset({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;return <main className="container" style={{maxWidth:520,padding:"50px 20px"}}><div className="panel"><h1>Reset password</h1>{token?<ActionForm endpoint="/api/auth/reset-password" extra={{token}} fields={[{name:"password",label:"Password baru (minimal 8 karakter)",type:"password",required:true}]} success="Password berhasil diganti. Silakan masuk." label="Ganti password"/>:<p>Tautan tidak valid. Minta tautan reset baru.</p>}<p><Link href="/login">Masuk</Link> · <Link href="/forgot-password">Minta tautan baru</Link></p></div></main>;}
